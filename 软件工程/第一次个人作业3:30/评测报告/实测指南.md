# 插件实测指南

> 目标：用实测数据支撑报告中的评分和结论。能定量的定量，不能定量的才定性描述。

---

## 一、性能测试（效率维度）— 全部量化

### 测试准备

记录测试环境：
- Blender 版本：\_\_\_\_\_\_
- CPU：\_\_\_\_\_\_
- 内存：\_\_\_\_\_\_
- 显卡（可选）：\_\_\_\_\_\_

### 生成速度测试

在同一区域面积下，测量插件从点击"生成"到场景完全呈现的时间。使用 Blender 内置的"区域覆盖"来界定城市范围。

| 场景规模 | 区域面积 | 生成耗时 | 生成的顶点总数 | 生成的物体数量 | Blender 内存峰值 |
|----------|----------|----------|----------------|----------------|------------------|
| 小规模   | 250m × 250m（0.0625 km²） | \_\_\_\_ 秒 | \_\_\_\_ 个 | \_\_\_\_ 个 | \_\_\_\_ MB |
| 中规模   | 500m × 500m（0.25 km²）  | \_\_\_\_ 秒 | \_\_\_\_ 个 | \_\_\_\_ 个 | \_\_\_\_ MB |
| 大规模   | 1km × 1km（1 km²）        | \_\_\_\_ 秒 | \_\_\_\_ 个 | \_\_\_\_ 个 | \_\_\_\_ MB |

> **测试方法**：关闭 Blender 的自动保存，关闭其他插件，用 Python 脚本计时：
> ```python
> import time
> start = time.time()
> # 执行生成操作
> print(f"生成耗时: {time.time() - start:.2f} 秒")
> ```
> 生成后查看右上角"场景统计"获取顶点数和物体数。

### 实时预览流畅度

在 UI 中连续拖动滑块调节参数（如建筑密度），观察预览帧率。

| 测试项 | 记录值 |
|--------|--------|
| 参数调节时 FPS | \_\_\_\_ FPS |
| 预览响应延迟 | 即时 / <1秒 / 1-3秒 / >3秒 / 无响应 |
| 预览过程中 Blender 是否卡死 | 是 / 否 |

---

## 二、功能性维度 — 量化列举 + 定性评价

### 2.1 支持的城市元素类型（计数）

在插件 UI 中逐一查看并统计：

| 元素类型 | iCity 数量/名称 | CityGenerator 数量/名称 |
|----------|-----------------|-------------------------|
| 道路类型（如主干道、次干道、高架路等） | \_\_\_\_ 种 | \_\_\_\_ 种 |
| 建筑类型（如住宅、商业、办公等） | \_\_\_\_ 种 | \_\_\_\_ 种 |
| 自然元素（如树木、草地、水体） | \_\_\_\_ 种 | \_\_\_\_ 种 |
| 交通设施（如路灯、公交站牌） | \_\_\_\_ 种 | \_\_\_\_ 种 |
| **合计** | \_\_\_\_ 种 | \_\_\_\_ 种 |

### 2.2 可调参数数量（计数）

在插件的参数面板中统计可调节的独立参数数量：

| 参数类别 | iCity 可调参数数 | CityGenerator 可调参数数 |
|----------|------------------|--------------------------|
| 道路相关（宽度、密度、网格尺寸等） | \_\_\_\_ 个 | \_\_\_\_ 个 |
| 建筑相关（高度、密度、风格等） | \_\_\_\_ 个 | \_\_\_\_ 个 |
| 地形相关（高程、平整度等） | \_\_\_\_ 个 | \_\_\_\_ 个 |
| 导出相关（格式、精度等） | \_\_\_\_ 个 | \_\_\_\_ 个 |
| **合计** | \_\_\_\_ 个 | \_\_\_\_ 个 |

> **记录方法**：截图参数面板，数一数滑块/输入框的数量。

### 2.3 导出格式支持（计数）

| 导出格式 | iCity 是否支持 | CityGenerator 是否支持 |
|----------|----------------|------------------------|
| OBJ | 支持 / 不支持 | 支持 / 不支持 |
| FBX | 支持 / 不支持 | 支持 / 不支持 |
| glTF / GLB | 支持 / 不支持 | 支持 / 不支持 |
| Blender 原生格式（.blend） | 支持 / 不支持 | 支持 / 不支持 |
| 其他（请注明） | | |
| **合计支持格式数** | \_\_\_\_ 种 | \_\_\_\_ 种 |

### 2.4 特殊功能（定性）

| 功能 | iCity | CityGenerator |
|------|-------|---------------|
| Google Maps / 真实地图导入 | 有 / 无 | 有 / 无 |
| 地形编辑工具 | 有 / 无 | 有 / 无 |
| 高架/立体道路 | 有 / 无 | 有 / 无 |
| 实时预览 | 有 / 无 | 有 / 无 |
| 随机种子控制（可复现） | 有 / 无 | 有 / 无 |

---

## 三、可靠性维度 — 量化失败率 + 定性描述

### 3.1 参数边界测试（量化）

对关键参数输入极端值，记录程序响应：

| 测试参数 | 极端值 | iCity 响应 | CityGenerator 响应 |
|----------|--------|------------|-------------------|
| 道路宽度 | 0 | 崩溃 / 报错 / 自动修正 / 正常 | 崩溃 / 报错 / 自动修正 / 正常 |
| 道路宽度 | 10000 | 崩溃 / 报错 / 自动修正 / 正常 | 崩溃 / 报错 / 自动修正 / 正常 |
| 建筑高度 | 0 | 崩溃 / 报错 / 自动修正 / 正常 | 崩溃 / 报错 / 自动修正 / 正常 |
| 建筑密度 | 0 | 崩溃 / 报错 / 自动修正 / 正常 | 崩溃 / 报错 / 自动修正 / 正常 |
| 城市规模 | 10km × 10km | 崩溃 / 报错 / 自动修正 / 正常 | 崩溃 / 报错 / 自动修正 / 正常 |

### 3.2 大规模场景稳定性（量化）

| 场景规模 | iCity 测试结果 | CityGenerator 测试结果 |
|----------|---------------|------------------------|
| 2km × 2km（4 km²）| 成功 / Blender 崩溃 / 生成错误 | 成功 / Blender 崩溃 / 生成错误 |
| 3km × 3km（9 km²）| 成功 / Blender 崩溃 / 生成错误 | 成功 / Blender 崩溃 / 生成错误 |
| 5km × 5km（25 km²）| 成功 / Blender 崩溃 / 生成错误 | 成功 / Blender 崩溃 / 生成错误 |

### 3.3 错误提示友好度（定性）

| 测试项 | iCity 评价 | CityGenerator 评价 |
|--------|------------|-------------------|
| 非法参数输入后是否有明确错误提示 | 有且友好 / 有但不明确 / 无 | 有且友好 / 有但不明确 / 无 |
| Blender 崩溃前是否有预警 | 有 / 无 | 有 / 无 |
| 撤销/重做功能是否正常 | 正常 / 部分正常 / 失效 | 正常 / 部分正常 / 失效 |

---

## 四、易用性维度 — 量化操作步骤 + 定性评价

### 4.1 任务完成效率（量化操作步骤）

记录完成同一任务所需的点击/操作次数：

| 任务 | iCity 操作步骤数 | CityGenerator 操作步骤数 |
|------|-----------------|-------------------------|
| 生成一个最简单的城市（使用默认参数） | \_\_\_\_ 步 | \_\_\_\_ 步 |
| 生成一个自定义城市（调整 3 个参数） | \_\_\_\_ 步 | \_\_\_\_ 步 |
| 导出生成的城市为 FBX 文件 | \_\_\_\_ 步 | \_\_\_\_ 步 |
| 重新生成（修改参数后） | \_\_\_\_ 步 | \_\_\_\_ 步 |

> **操作步骤定义**：每次鼠标点击、拖动滑块、输入数值均计为 1 步。

### 4.2 学习曲线（量化时间）

| 测试项 | iCity | CityGenerator |
|--------|-------|---------------|
| 阅读文档后生成第一个城市的时间 | \_\_\_\_ 分钟 | \_\_\_\_ 分钟 |
| 不读文档、凭直觉摸索生成第一个城市的时间 | \_\_\_\_ 分钟 | \_\_\_\_ 分钟 |
| 掌握全部主要功能的时间（粗估） | \_\_\_\_ 小时 | \_\_\_\_ 小时 |

### 4.3 UI 评价（定性）

| 评价维度 | iCity | CityGenerator |
|----------|-------|---------------|
| 参数面板布局是否清晰 | 清晰 / 一般 / 混乱 | 清晰 / 一般 / 混乱 |
| 参数名称是否有中文注释 | 全部有 / 部分有 / 无 | 全部有 / 部分有 / 无 |
| 是否有操作引导提示 | 有 / 无 | 有 / 无 |

---

## 五、维护性维度（仅 CityGenerator 可深度测）

| 测试项 | CityGenerator |
|--------|---------------|
| 代码总行数（Python） | \_\_\_\_ 行 |
| 代码是否有模块划分（.py 文件数） | \_\_\_\_ 个文件 |
| 是否有注释 | 有（覆盖 XX%） / 极少 / 无 |
| 是否提供 API / 扩展接口 | 有 / 无 |
| 二次开发（如修改道路生成算法）难度评估 | 简单 / 中等 / 困难 |
| GitHub 最后更新时间 | \_\_\_\_ 年\_\_月 |
| GitHub Star 数 | \_\_\_\_ |

---

## 六、可移植性维度

| 测试项 | iCity | CityGenerator |
|--------|-------|---------------|
| 支持的 Blender 主版本数 | \_\_\_\_ 个（如 3.x, 4.x） | \_\_\_\_ 个 |
| 安装是否一次成功 | 是 / 否 | 是 / 否 |
| 是否需要额外依赖 | 是（注明） / 否 | 是（注明） / 否 |
| 与其他常用插件是否冲突（Archimesh 等） | 冲突 / 未测试 / 无冲突 | 冲突 / 未测试 / 无冲突 |

---

## 七、综合评分表（基于实测数据）

| 维度 | 量化指标 | iCity 评分 | CityGenerator 评分 | 评分依据 |
|------|----------|-----------|---------------------|----------|
| **功能性** | 支持元素种类数 / 可调参数数 / 导出格式数 | \_\_\_\_ / 5 | \_\_\_\_ / 5 | 数据：iCity \_\_\_\_ vs CG \_\_\_\_ |
| **可靠性** | 大规模场景成功率 / 错误处理友好度 | \_\_\_\_ / 5 | \_\_\_\_ / 5 | 数据：iCity 成功率 \_\_\_\_% vs CG \_\_\_\_% |
| **易用性** | 任务操作步骤数 / 首次上手时间 | \_\_\_\_ / 5 | \_\_\_\_ / 5 | 数据：iCity 上手 \_\_\_\_min vs CG \_\_\_\_min |
| **效率** | 生成速度 / 内存占用 / 实时预览 FPS | \_\_\_\_ / 5 | \_\_\_\_ / 5 | 数据：1km² 生成 iCity \_\_\_\_s vs CG \_\_\_\_s |
| **维护性** | 开源 / 模块化 / 社区活跃度 | \_\_\_\_ / 5 | \_\_\_\_ / 5 | 定性：iCity 商业迭代 vs CG 开源可改 |
| **可移植性** | Blender 版本支持数 / 安装成功率 | \_\_\_\_ / 5 | \_\_\_\_ / 5 | 数据：iCity 支持 \_\_\_\_ 个版本 vs CG \_\_\_\_ 个 |
| **综合总分** | — | \_\_\_\_ / 30 | \_\_\_\_ / 30 | |

---

## 反馈格式

```
测试环境：
- Blender 版本：
- CPU：
- 内存：
- 显卡：

【性能数据】
| 规模 | iCity 耗时 | CG 耗时 | iCity 顶点数 | CG 顶点数 |
|------|-----------|---------|-------------|-----------|
| 250m² |   秒     |   秒    |     个      |     个    |
| 500m² |   秒     |   秒    |     个      |     个    |
| 1km²  |   秒     |   秒    |     个      |     个    |

【功能数据】
- iCity 支持元素种类：___ 种
- CityGenerator 支持元素种类：___ 种
- iCity 可调参数：___ 个
- CityGenerator 可调参数：___ 个
- iCity 导出格式：___ 种
- CityGenerator 导出格式：___ 种

【可靠性数据】
- iCity 大规模（2km²）测试：成功/崩溃
- CityGenerator 大规模（2km²）测试：成功/崩溃
- 极端参数测试 iCity：___ 次崩溃 / ___ 次报错 / 0次崩溃
- 极端参数测试 CG：___ 次崩溃 / ___ 次报错 / 0次崩溃

【易用性数据】
- 生成默认城市：iCity ___ 步，CG ___ 步
- 首次上手时间：iCity ___ 分钟，CG ___ 分钟

【维护性数据】（仅 CG）
- 代码行数：___ 行
- 文件数：___ 个
- GitHub Star：___

【评分调整】（对比预设评分）
- 功能性：预设 iCity 5分 → 实测建议 ___ 分
- 效率：预设 iCity 4分 → 实测建议 ___ 分
- ……

【其他发现】
（任何你觉得有价值但上面没覆盖的信息）
```
