借鉴3.2的实验，实现三个人对他们拥有的数据的平均值的计算，下面是3.2实验的内容。


### 实验目的

* 掌握Shamir秘密共享在隐私计算中的应用。
* 了解如何在多人投票情况下统计票数而不泄露个体投票信息。

### 实验背景

有三个学生（Student₁、Student₂、Student₃）需要对班里的优秀干部（Alice、Bob、Charles、Douglas）进行投票。每个学生对每个候选人有一个投票（0或1），实验要求在不暴露各自投票内容的前提下，统计每个候选人的总票数。

### 实验要求

1. **秘密分割**

   * 每个投票学生对自己的投票值使用Shamir秘密共享生成三个秘密份额 (a_i, b_i, c_i)。
   * 每个学生将自己的投票值分割为三个份额，并按照实验图示分发给其他两名学生。

2. **门限共享**

   * 实验采用 (2,3) 门限共享：任意两个份额可以重构原始投票值。

3. **投票员**

   * 在三个投票学生之外，选择一个投票员来统计总票数。
   * 投票员无需知道单个学生的投票。

4. **秘密共享传输**

   * 每个学生将自己生成的三个份额分别发送给其他学生或投票员。
   * 投票员将收到的份额进行累加，得到每个候选人的票数之和：
     [
     d_1 = a_1 + b_1 + c_1, \quad
     d_2 = a_2 + b_2 + c_2, \quad
     d_3 = a_3 + b_3 + c_3
     ]

5. **票数重构**

   * 投票员从每个候选人处随机选择任意两个份额进行重构。
   * 计算每个候选人的总票数。

6. **实验输出**

   * 输出每个候选人的最终票数。
   * 验证通过秘密共享统计的总票数与直接相加结果一致。

---

### 注意事项

* 每个投票值只能是 0 或 1。
* 每个学生必须对三个份额独立加密或生成。
* 投票员不应知道任何单个学生的具体投票。

